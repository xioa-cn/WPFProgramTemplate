<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
                    xmlns:theme="clr-namespace:MachineApplication.Entrance.Theme"
                    xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/MachineApplication.Entrance;component/Theme/ColorAdjustmentStyles.xaml" />
    </ResourceDictionary.MergedDictionaries>
    <!--  主题切换只保留图标，避免默认按钮模板产生背景和边框。  -->
    <Style x:Key="ThemeModeIconButton" TargetType="Button">
        <Setter Property="Width" Value="32" />
        <Setter Property="Height" Value="32" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <!-- 透明表面覆盖整个按钮，图标周围留白也可以点击。 -->
                    <Border Background="Transparent">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="ColorButton" TargetType="Button">
        <Setter Property="Width" Value="54" />
        <Setter Property="Height" Value="46" />
        <Setter Property="Margin" Value="0,0,6,6" />
        <Setter Property="Background" Value="{Binding Background}" />
        <Setter Property="Foreground" Value="{Binding Foreground}" />
        <Setter Property="ToolTip" Value="{Binding Label}" />
        <Setter Property="AutomationProperties.Name" Value="{Binding Label}" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border
                        x:Name="Tile"
                        Background="{TemplateBinding Background}"
                        BorderBrush="#18000000"
                        BorderThickness="2"
                        CornerRadius="7">
                        <Border.RenderTransform>
                            <TranslateTransform />
                        </Border.RenderTransform>
                        <Grid>
                            <!-- 只在边缘加高光，不覆盖色块主体，保留色盘颜色的准确性。 -->
                            <Border BorderBrush="#50FFFFFF" BorderThickness="0,1,0,0" CornerRadius="5"
                                    IsHitTestVisible="False" />
                            <TextBlock
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                FontSize="12"
                                Text="{Binding Name}" />
                            <materialDesign:PackIcon
                                x:Name="Check"
                                Width="12"
                                Height="12"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Top"
                                Kind="Check"
                                Visibility="Collapsed" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <DataTrigger Binding="{Binding IsSelected}" Value="True">
                            <Setter TargetName="Tile" Property="BorderBrush" Value="{Binding Foreground}" />
                            <Setter TargetName="Check" Property="Visibility" Value="Visible" />
                        </DataTrigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Tile" Property="RenderTransform">
                                <Setter.Value>
                                    <TranslateTransform Y="-1" />
                                </Setter.Value>
                            </Setter>
                            <Setter TargetName="Tile" Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.18" />
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Tile" Property="Opacity" Value="0.85" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="Tile" Property="BorderBrush" Value="{Binding Foreground}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <DataTemplate x:Key="PaletteTemplate">
        <StackPanel Margin="0,0,0,14">
            <TextBlock
                Margin="0,0,0,8"
                FontWeight="SemiBold"
                Text="{Binding DisplayName}" />
            <ItemsControl ItemsSource="{Binding Colors}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button
                            Command="{x:Static theme:ThemeColorCommands.Apply}"
                            CommandParameter="{Binding RelativeSource={RelativeSource Self}}"
                            Style="{StaticResource ColorButton}"
                            Tag="{Binding DataContext.SelectColorCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </DataTemplate>
</ResourceDictionary>