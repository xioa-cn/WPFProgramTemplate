<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:models="clr-namespace:MachineApplication.Entrance.Models"
                    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
        <BooleanToVisibilityConverter x:Key="BooleanVisibility" />
        <!--  模板按节点类型递归应用；每一级只负责自己的缩进与展开状态。  -->
        <DataTemplate DataType="{x:Type models:NavModel}">
            <StackPanel>
                <Button
                    MinHeight="46"

                    HorizontalContentAlignment="Stretch"
                    AutomationProperties.Name="{Binding Title}"
                    Command="{Binding DataContext.ActivateNavigationCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    CommandParameter="{Binding}"
                    ToolTip="{Binding Title}">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Margin" Value="0,3,8,3" />
                            <Setter Property="Foreground" Value="{DynamicResource MaterialDesign.Brush.Foreground}" />
                            <Setter Property="Background" Value="Transparent" />
                            <Setter Property="BorderThickness" Value="0" />
                            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border
                                            x:Name="Row"
                                            Background="{TemplateBinding Background}"
                                            CornerRadius="0,24,24,0">
                                            <Grid>
                                                <Border
                                                    x:Name="Highlight"
                                                    Background="{DynamicResource MaterialDesign.Brush.Foreground}"
                                                    CornerRadius="0,24,24,0"
                                                    Opacity="0" />
                                                <Grid x:Name="MenuContent" Margin="14,8,8,8">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="22" />
                                                        <ColumnDefinition Width="*" />
                                                        <ColumnDefinition x:Name="ChevronColumn" Width="30" />
                                                    </Grid.ColumnDefinitions>
                                                    <materialDesign:PackIcon
                                                        Width="18"
                                                        Height="18"
                                                        VerticalAlignment="Center"
                                                        Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"
                                                        x:Name="MenuIcon" Kind="{Binding Icon}" />
                                                    <TextBlock x:Name="MenuLabel"
                                                        Grid.Column="1"
                                                        Margin="10,0,4,0"
                                                        VerticalAlignment="Center"
                                                        Text="{Binding Title}"
                                                        TextTrimming="CharacterEllipsis" />
                                                    <materialDesign:PackIcon
                                                        x:Name="Chevron"
                                                        Grid.Column="2"
                                                        Width="18"
                                                        Height="18"
                                                        HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"
                                                        Kind="ChevronRight"
                                                        Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"
                                                        Visibility="{Binding HasChildren, Converter={StaticResource BooleanVisibility}}" />

                                                </Grid>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <DataTrigger Binding="{Binding DataContext.IsNavigationCollapsed, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
                                                <Setter Property="Margin" Value="0,3,0,3" />
                                                <Setter TargetName="MenuContent" Property="Margin" Value="0,8" />
                                                <Setter TargetName="MenuIcon" Property="Grid.ColumnSpan" Value="3" />
                                                <Setter TargetName="MenuIcon" Property="HorizontalAlignment" Value="Center" />
                                                <Setter TargetName="MenuLabel" Property="Visibility" Value="Collapsed" />
                                                <Setter TargetName="Chevron" Property="Visibility" Value="Collapsed" />
                                                <Setter TargetName="ChevronColumn" Property="Width" Value="0" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsExpanded}" Value="True">
                                                <Setter TargetName="Chevron" Property="Kind" Value="ChevronDown" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                <Setter TargetName="Highlight" Property="Opacity" Value="0" />
                                                <Setter Property="FontWeight" Value="SemiBold" />

                                            </DataTrigger>
                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter TargetName="Row" Property="Opacity" Value="0.7" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
                <ItemsControl
                    Margin="12,0,0,0"
                    ItemsSource="{Binding Children}"
                    >
                    <ItemsControl.Style>
                        <Style TargetType="ItemsControl">
                            <Setter Property="Visibility" Value="{Binding IsExpanded, Converter={StaticResource BooleanVisibility}}" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding DataContext.IsNavigationCollapsed, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ItemsControl.Style>
                </ItemsControl>
            </StackPanel>
        </DataTemplate>
    </ResourceDictionary>